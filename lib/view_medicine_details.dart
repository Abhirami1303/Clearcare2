import 'package:flutter/material.dart';

class ViewMedicineDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View Medicine Details')),
      body: Center(
        child:
            // Add widgets to display medicine details here
            Text('View Medicine Details Page'),
      ),
    );
  }
}
